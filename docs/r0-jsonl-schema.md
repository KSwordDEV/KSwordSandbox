# R0Collector JSONL schema

R0Collector writes newline-delimited JSON. Each line is one `SandboxEvent`-
compatible object and is safe to merge with Guest Agent `events.json`.

## Top-level object

Required fields:

- `eventType`: string. Collector lifecycle/error type or normalized driver
  event type.
- `source`: string. `r0collector` for collector lifecycle rows, `driver` for
  drained or synthetic driver-category rows.
- `timestamp`: UTC ISO-8601 string generated by the collector.
- `processId`: number. Driver PID when the event header/payload supplies one;
  otherwise the collector PID.
- `path`: string. Device path for lifecycle rows; typed file/process/image/
  registry rows use the decoded subject path when available.
- `commandLine`: string. Collector command line for lifecycle rows; synthetic
  rows use the mock process command line.
- `data`: object. Values are emitted as strings for compatibility with the
  current host `Dictionary<string,string>` model.

Example:

```json
{"eventType":"r0collector.started","source":"r0collector","timestamp":"2026-07-10T00:00:00.000Z","processId":1234,"path":"\\\\.\\KSwordSandboxDriver","commandLine":"KSword.Sandbox.R0Collector.exe --self-test --output -","data":{"devicePath":"\\\\.\\KSwordSandboxDriver","outputPath":"-","durationSeconds":"0","pollIntervalMs":"500","mockMode":"true","syntheticMode":"true","heartbeat":"false","enableMaskSpecified":"false","enableMask":"0","enableMaskHex":"0x00000000","ioctlProtocol":"KSwordSandboxDriverIoctl.h"}}
```

## Collector lifecycle rows

- `r0collector.started`: first normal row after options and output are valid.
  `data` includes `devicePath`, `outputPath`, `durationSeconds`,
  `pollIntervalMs`, `mockMode`, `syntheticMode`, `heartbeat`,
  `enableMaskSpecified`, `enableMask`, `enableMaskHex`, and `ioctlProtocol`.
- `r0collector.heartbeat`: emitted only with `--heartbeat`. `data.reason` is
  `collectorStarted`, `pollComplete`, or `syntheticComplete`.
- `r0collector.deviceOpened`: emitted after `CreateFileW` succeeds. Includes the
  selected `enableMask` and ABI protocol name.
- `r0collector.driverHealth`: successful
  `IOCTL_KSWORD_SANDBOX_GET_HEALTH` reply.
- `r0collector.driverPoll`: successful `IOCTL_KSWORD_SANDBOX_POLL` reply.
- `r0collector.driverReadEvents`: batch summary for successful
  `IOCTL_KSWORD_SANDBOX_READ_EVENTS`.
- `r0collector.stopped`: final row on successful synthetic, one-shot, or timed
  completion.

Error rows:

- `r0collector.argumentError`: invalid CLI.
- `r0collector.outputUnavailable`: selected JSONL sink could not be opened.
- `r0collector.deviceUnavailable`: driver device could not be opened.
- `r0collector.ioctlFailure`: `DeviceIoControl` returned a Win32 failure.
- `r0collector.driverProtocolError`: an IOCTL succeeded but returned malformed
  or incompatible bytes.

## Driver event rows

Rows drained from `IOCTL_KSWORD_SANDBOX_READ_EVENTS` use `source:"driver"` and
these normalized `eventType` values:

- `driver.load`
- `driver.process`
- `image.load`
- `driver.file`
- `driver.registry`
- `driver.network`
- `driver.event.reserved`
- `driver.event.none`
- `driver.event` for forward-compatible unknown types

Common `data` fields include `ioctl`, `batchIndex`, `recordOffset`, `version`,
`versionHex`, `recordSize`, `driverEventType`, `driverEventTypeName`, `flags`,
`flagsHex`, `sequence`, `timestampQpc`, `driverProcessId`, `driverThreadId`,
`payloadSize`, `payloadHexBytes`, `payloadTruncated`, `payloadHex`, and
`typedPayloadParsed`.

Typed payload parsers add category-specific fields such as `operationName`,
`filePath`, `imagePath`, `keyPath`, `valueName`, `protocolName`,
`directionName`, `localAddress`, `remoteAddress`, and port metadata when the
driver payload ABI provides them.

## Synthetic/self-test rows

`--mock`, `--synthetic`, and `--self-test` do not open the driver. They emit:

- `r0collector.started`
- optional `r0collector.heartbeat`
- `r0collector.mockDriverEvent`
- synthetic `driver.process`
- synthetic `image.load`
- synthetic `driver.file`
- synthetic `driver.registry`
- optional `r0collector.heartbeat`
- `r0collector.stopped`

Synthetic driver-category rows have `data.mock:"true"` and
`data.typedPayloadStatus:"mock"` so host reports can distinguish plumbing tests
from real R0 telemetry.

## CLI fields that affect JSONL

- `--duration` controls whether the driver path is one-shot (`0`) or timed.
- `--poll-ms` / `--poll-interval-ms` controls sleep between timed poll/read
  iterations.
- `--enable-mask <mask>` accepts decimal or `0x` 32-bit values, passes the value
  in `KSWORD_SANDBOX_READ_EVENTS_REQUEST.Flags`, and records it in lifecycle and
  heartbeat rows.
- `--heartbeat` adds `r0collector.heartbeat` rows without changing driver drain
  semantics.
